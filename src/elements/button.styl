[class*='button']
  --background-color #fff
  --background-color--active #eee
  --background-color--disabled #eee
  --background-color--hover #eee
  --background-image none
  --background-image--active none
  --background-image--disabled none
  --background-image--hover none
  --border-color #bababa
  --border-color--active #bababa
  --border-color--disabled #bababa
  --border-color--hover #bababa
  --border-radius calc(var(--font-size) / var(--border-radius-divider))
  --border-radius-divider 5
  --border-style solid
  --border-width 1px
  --color #333
  --color--active #444
  --color--disabled #444
  --color--hover #444
  --cursor pointer
  --line-height calc(var(--vertical-rhythm) * 4)
  background-color var(--background-color)
  background-image var(--background-image)
  border-color var(--border-color)
  border-radius var(--border-radius)
  border-style var(--border-style)
  border-width var(--border-width)
  color var(--color)
  cursor var(--cursor)
  font-size var(--font-size)
  line-height calc(var(--line-height) - calc( var(--border-width) * 2 ))
  margin 0
  margin-left var(--vertical-rhythm)
  margin-right @margin-left
  padding-bottom 0
  padding-left var(--font-size)
  padding-right @padding-left
  padding-top 0
  text-align center
  text-decoration none

  // Display tags A and SPAN as inline block to respect the paddings / margins / height
  a&, span&
    display inline-block

  // States
  &:hover,  &.is-hover
    --background-color var(--background-color--hover)
    --background-image var(--background-image--hover)
    --border-color var(--border-color--hover)
    --color var(--color--hover)

  &:active, &.is-active, &:focus, &.is-focused
    --background-color var(--background-color--active)
    --background-image var(--background-image--active)
    --border-color var(--border-color--active)
    --color var(--color--active)

  &:disabled, &.is-disabled
    --background-color var(--background-color--disabled)
    --background-image var(--background-image--disabled)
    --border-color var(--border-color--disabled)
    --color var(--color--disabled)
    --cursor default

// Generate buttons in all sizes
buttonDefault()
  &button
    for $size in $sizes
      &-{$size}
        --font-size var(--font-size-- + $size)
        --line-height var(--line-height-- + $size)

// Generate buttons in all sizes in all breakpoints
+generateForBreakpoints()
  buttonDefault()
